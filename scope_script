# !/bin/bash
# Author: Martín Gil Blanco

# El script se va a ejecutar un cambio de directorio pero no va a afectar
# al prompt ya que uando ejecutas un script en un entorno de shell, se 
# ejecuta en un subproceso o subshell. El subshell hereda las variables y 
# el entorno del shell principal solo durante la ejecución del script. 
# Una vez que el script termina, el subshell también termina, y el estado
# del entorno no afecta al shell principal.

pwd
cd $HOME
echo "Dirigiendose a home"
pwd
